
import './App.css';
import { MainRoutes } from './Pages/MainRoutes';

// Main routes is also a page
// use custom hooks and develop logics in custom hooks to get promoted in awesome way

function App() {
  return (
    <div className="App">
      <MainRoutes />
    </div>
  );
}

export default App;
